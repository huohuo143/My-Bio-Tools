{
  "name": "my-bio-tools-auth-service",
  "version": "1.9.1",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --experimental-strip-types --check src/index.ts",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply my-bio-tools-auth --local",
    "db:migrate:remote": "wrangler d1 migrations apply my-bio-tools-auth --remote"
  },
  "devDependencies": {
    "wrangler": "4.36.0"
  }
}
